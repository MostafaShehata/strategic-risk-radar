# Strategic Risk Radar Database

PostgreSQL container and schema for raw documents, ingestion runs, source
execution details, and per-keyword metrics.

Run `./build.ps1` and `./deploy.ps1` from this directory on Windows.

